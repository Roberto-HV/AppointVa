services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: appointva
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - appointva_pgdata:/var/lib/postgresql/data

volumes:
  appointva_pgdata:
